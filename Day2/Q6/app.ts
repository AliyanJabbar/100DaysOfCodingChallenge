export {}
let name: string = "\t\n Aliyan \t\n"   // This saves the name with whitespace
console.log(name)    // Shows the name with whitespace
console.log(name.trim())  // Shows the name without whitespace or it cleans the text.